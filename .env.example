# ─────────────────────────────────────────────
# MEALGAME 환경변수
# 이 파일을 .env 로 복사해서 사용하세요. (.env 는 커밋되지 않습니다)
# 아무것도 설정하지 않아도 앱은 "로컬 모드"로 완전히 동작합니다.
# ─────────────────────────────────────────────

# [실시간 동기화] Supabase
# 설정하지 않으면 같은 기기의 여러 탭끼리만 동기화되는 로컬 모드로 동작합니다.
# supabase/schema.sql 을 프로젝트 SQL Editor 에 실행한 뒤 아래 값을 채우세요.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# [식당 데이터] mock | kakao
# mock: 내장 목업 데이터(기본값) / kakao: 카카오 로컬 API
VITE_PLACES_PROVIDER=mock
# 카카오 REST API 키 (VITE_PLACES_PROVIDER=kakao 일 때만 필요)
# 주의: VITE_ 접두사 값은 브라우저에 노출됩니다. 운영에서는 프록시 서버를 두세요.
VITE_KAKAO_REST_API_KEY=

# [AI 판사] mock | http
# mock: 결정론적 목업 판결(기본값)
# http: 아래 엔드포인트로 판결을 위임 (LLM API 키는 반드시 서버에 보관)
VITE_AI_JUDGE_PROVIDER=mock
VITE_AI_JUDGE_ENDPOINT=
